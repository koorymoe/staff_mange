# رفع النظام على سيرفر Hetzner — خطوة بخطوة

## 1. إنشاء السيرفر
- Hetzner Cloud Console → Add Server
- الموقع: أقرب مكان (مثلاً Falkenstein/Nuremberg ألمانيا أو Ashburn أمريكا)
- الصورة (Image): **Ubuntu 24.04**
- النوع: **CX22**
- SSH Key: أضف مفتاح SSH (أو استخدم كلمة سر مؤقتة تظهر بالإيميل)
- اضغط Create & Buy Now

## 2. الاتصال بالسيرفر
```
ssh root@<IP السيرفر>
```

## 3. تثبيت Docker (مرة وحدة بس)
```bash
curl -fsSL https://get.docker.com | sh
```

## 4. تحميل المشروع
```bash
git clone <رابط الريبو> staff_mange
cd staff_mange
git checkout claude/zen-mendel-70b9j2
```

## 5. إعداد الإعدادات
```bash
cp .env.production.example .env
nano .env   # عبّي DB_PASSWORD و JWT_SECRET و CORS_ORIGIN و OWNER_USERNAME/OWNER_PASSWORD
```
- `JWT_SECRET`: نص عشوائي طويل — تكدر تولّده بـ `openssl rand -hex 32`
- `CORS_ORIGIN`: رابط الموقع (مثلاً `http://<IP السيرفر>` مبدئياً، وبعدين الدومين لما يجهز)
- `OWNER_USERNAME` و `OWNER_PASSWORD`: بيانات دخول حساب المالك (OWNER) — أعلى صلاحية بالنظام، فوق ADMIN،
  ويشوف لوحة المراقبة الخلفية الحصرية. هذي المتغيرات **حصراً** المصدر اللي يحدد بيانات دخول المالك —
  ما فيه أي قيمة افتراضية بالكود (كانت هذي ثغرة أمنية سابقاً وانصلحت).
  - **أول تشغيل** (أو أي سيرفر جديد): لازم تعبيهم، وإلا ما يتزرع حساب المالك إطلاقاً وما تكدر تدخل كـOWNER.
  - **التحديثات اللاحقة على سيرفر شغّال أصلاً**: لو خليتهم فاضيين بـ`.env`، حساب المالك الموجود بقاعدة
    البيانات **ما ينلمس أبداً** (يبقى بنفس اسم المستخدم/كلمة المرور الحالية). لو عبّيتهم بقيم، حساب
    المالك ينبني أو يتحدّث ليطابق بالضبط القيم اللي حطيتها (يعني لو تنسى تحدّث `.env` بعد ما تغيّر كلمة
    مرور المالك من مكان ثاني، أول `deploy.sh` جاي يرجّعها لقيمة `.env` القديمة — خليها متزامنة دائماً).
  - ما تحط بيانات دخول ضعيفة أو افتراضية هنا؛ نفس مستوى الحساسية مثل `JWT_SECRET`/`DB_PASSWORD`.

### الدومين (`DOMAIN`) — لو عندك دومين جاهز
لو بديت مباشرة بدومين حقيقي (بدل `http://<IP السيرفر>` المؤقت)، عبّي متغير `DOMAIN` بـ`.env`
(مثلاً `DOMAIN=example.com`). خدمة `caddy` بـ`docker-compose.yml` تقرأ هذا المتغير وتستخدمه بملف
`Caddyfile` (`{$DOMAIN} { reverse_proxy frontend:80 }`) — Caddy يطلب شهادة HTTPS مجانية من
Let's Encrypt تلقائياً لهذا الدومين ويجدّدها لحاله بدون أي تدخل يدوي، بشرط إن سجل A بالدومين موجّه
فعلاً لـ IP السيرفر قبل التشغيل. لو ما عندك دومين بعد، خليه فاضي واستخدم `http://<IP السيرفر>` مؤقتاً
(نفس خطوة 8 بالأسفل).

## 6. التشغيل
```bash
./deploy.sh
```
أول مرة تاخذ بضع دقائق (تحميل وبناء الصور). بعدها النظام يشتغل على:
- الموقع: `http://<IP السيرفر>` (بورت 80)
- الباك إند وراء نفس الموقع تلقائياً على `/api`

## 7. كل تحديث لاحق (بعد ما تسوي تعديلات وتدزها Github)
على السيرفر فقط:
```bash
cd staff_mange
./deploy.sh
```

## 8. ربط دومين (اختياري، بعدين)
لما ياخذ الدومين، وجّه سجل A بالدومين لـ IP السيرفر، وبعدها نضيف HTTPS مجاني (Let's Encrypt).

## ملاحظات أمان
- ما تحط ملف `.env` بالـ git أبداً (موجود بالفعل ضمن `.gitignore`).
- خلي `JWT_SECRET` و `DB_PASSWORD` قويين وما تشاركهم مع أحد.
