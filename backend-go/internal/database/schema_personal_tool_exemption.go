package database

// ══════════════════════════════════════════════════════════════════
// استثناء أداة من عدة موظف بعينه
// ══════════════════════════════════════════════════════════════════
//
// شكوى أبو الكميات: «من احذف أداة من الأدوات الخاصة بموظف معين
// أريدها تنحذف من هاي القائمة، ماريدها تبقى خالصة من المخزن —
// يريد يحذفها».
//
// ⚠️ السبب الحقيقي: «الناقص» يتحسب = **أسماء العدة القياسية** ناقص
// الي يملكه الموظف. فحذف الأداة من عدته **هو بالضبط الي يخلقها
// نقصاً** — الاسم لسه موجود بالقالب القياسي، فترجع فوراً بتقرير
// النواقص. مو خللاً بالتحديث ولا كاش قديم: هذا سلوكه المصمَّم.
//
// ⚠️ وليش ما نحذفها من القالب القياسي بدلها؟ لأن القالب **مشترك بين
// كل الفنيين**: حذف «بريمة جدارية» منه يعني ماكو ولا فني يتحاسب
// عليها. أبو الكميات يريدها تنشال من عدة **هذا الموظف** بس.
//
// فالحل استثناء لكل موظف: الأداة تبقى بالقالب لغيره، وتنشال من
// نواقص هذا الموظف وحده.
//
// ⚠️ والاستثناء **قابل للتراجع** — صف ينمحى فترجع الأداة لنواقصه.
// قرار ما ينرجع منه يخلي أبو الكميات يخاف يضغط الزر.
//
// ⚠️ والمطابقة بالاسم النصي (نفس ما يتحسب النقص) — لهذا نخزن الاسم
// مقصوصاً، ونطابق مقصوصاً، حتى «مفتاح » و«مفتاح» ما يصيرون اثنين.
func personalToolExemptionMigrations() []Migration {
	return []Migration{
		{
			Version: "0268_personal_tool_exemption",
			SQL: `
				CREATE TABLE IF NOT EXISTS "PersonalToolExemption" (
					id TEXT PRIMARY KEY,
					"employeeId" TEXT NOT NULL REFERENCES "Employee"(id) ON DELETE CASCADE,
					"toolName" TEXT NOT NULL,
					note TEXT,
					-- ⚠️ SET NULL مو CASCADE: حذف الموظف الي استثنى ما
					-- يمحي إن الاستثناء صار — بس اسمه منسوخ نصاً حتى
					-- يبقى السطر مقروءاً.
					"byEmployeeId" TEXT REFERENCES "Employee"(id) ON DELETE SET NULL,
					"byName" TEXT NOT NULL DEFAULT '',
					"createdAt" TIMESTAMP NOT NULL DEFAULT now()
				)`,
		},
		{
			Version: "0268_personal_tool_exemption_unique",
			// أداة وحدة ما تنستثنى مرتين لنفس الموظف — وإلا حذف/إرجاع
			// متكرر يخلّف صفوفاً مكرّرة والإرجاع ما يشيلها كلها.
			SQL: `CREATE UNIQUE INDEX IF NOT EXISTS "PersonalToolExemption_emp_tool_idx"
			      ON "PersonalToolExemption"("employeeId", "toolName")`,
		},
	}
}
