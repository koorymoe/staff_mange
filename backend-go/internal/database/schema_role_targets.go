package database

// ══════════════════════════════════════════════════════════════════
// أهداف المقاييس لكل دور — أساس شاشة إحصائيات الموظفين
// ══════════════════════════════════════════════════════════════════
//
// طلب (ع): ورقة الإكسل الي يشتغل بيها بالإيد فيها لكل مقياس **ثلاثة
// أعمدة: الهدف · المنجز · النسبة**. والنظام چان يعرض المنجز بس —
// فالنسبة (وهي الي يتخذ عليها القرار) تنحسب بالإيد بالورقة.
//
// ⚠️ **ماكو ولا جدول أهداف ولا إعدادات بكل السكيما** (مسحت أسماء
// الجداول كلها) — فهذا أول جدول من نوعه، والنسبة مستحيلة بدونه.
//
// ⚠️ **الهدف لكل دور مو لكل موظف**: قرار (ع) صراحةً. يكتبه مرة وحدة
// للفني والليدر والإداري وينفع لكل الأشهر ولكل موظف بذاك الدور،
// ويعدّله وقت ما يريد.
//
// ⚠️ **وما يُحسب من متوسط الماضي** — وهذا رفضناه بقصد: لو الشهر
// الماضي ضعيف يصير الهدف ضعيفاً، والنسبة تطلع عالية **كاذبة**،
// والموظف الضعيف يبين ناجحاً.
//
// ⚠️ **والهدف الفاضي (ماكو صف) يعني «ماكو نسبة»** — تُعرض «—» مو
// ٠٪ ولا ١٠٠٪. رقم غلط أسوأ من ماكو رقم، وهاي شاشة تُبنى عليها
// قرارات رواتب.
//
// ⚠️ **`metricKey` نص مو enum بالقاعدة**: المقاييس تُعرَّف بخريطة Go
// وحدة، فإضافة مقياس جديد ما تحتاج ترحيلاً ولا تعديل قاعدة. والقيود
// على المفتاح تبقى بالكود (الخريطة هي المرجع).
func roleMetricTargetMigrations() []Migration {
	return []Migration{
		{
			Version: "0275_role_metric_target",
			SQL: `
				CREATE TABLE IF NOT EXISTS "RoleMetricTarget" (
					id TEXT PRIMARY KEY DEFAULT gen_random_uuid()::text,
					-- الدور بالنص نفسه الي بـ"Employee".role
					role TEXT NOT NULL,
					-- مفتاح المقياس — مرجعه خريطة المقاييس بالكود
					"metricKey" TEXT NOT NULL,
					-- ⚠️ NUMERIC مو INTEGER: أكو أهداف مبالغ (٧٠٠٠٠٠)
					-- وأكو نسب وأكو أعداد صغيرة، والكسر يلزم بالنسب.
					"targetValue" NUMERIC(14,2) NOT NULL,
					-- ⚠️ SET NULL مو CASCADE: حذف الموظف الي كتب الهدف
					-- ما يمحي الهدف نفسه — الهدف قرار إداري يبقى.
					"updatedById" TEXT REFERENCES "Employee"(id) ON DELETE SET NULL,
					"updatedByName" TEXT NOT NULL DEFAULT '',
					"updatedAt" TIMESTAMP NOT NULL DEFAULT now()
				)`,
		},
		{
			Version: "0275_role_metric_target_unique",
			// هدف واحد لكل (دور، مقياس) — وإلا صفّان متناقضان والشاشة
			// تختار واحداً عشوائياً، فالنسبة تتغيّر بلا سبب ظاهر.
			SQL: `CREATE UNIQUE INDEX IF NOT EXISTS "RoleMetricTarget_role_metric_idx"
			      ON "RoleMetricTarget"(role, "metricKey")`,
		},
	}
}
