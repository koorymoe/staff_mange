package database

// ═══ صلاحيات مستقلة بدل الركوب على صلاحية عامة ═══
//
// المشكلة: شاشات كثيرة كانت تنفتح بصلاحية عامة مثل `staff_management`
// أو `coordinator`. يعني تنطي موظف صلاحية «تنسيق الحجوزات» عشان
// يشتغل بالتنسيق — فينفتحله معاها **أرشيف الحجوزات** كله بلا ما
// تقصد، لأن الاثنين على نفس المفتاح.
//
// وهذا يخالف طلب صاحب العمل: «الموظف ما يكدر يسوي شي إلا إذا انطيته
// الصلاحية مال هذا الشي» — صلاحية لكل شغلة، مو مفتاح عام يفتح باب
// وراه أبواب.
//
// ⚠️ ما ننطي الصلاحيات الجديدة لأحد تلقائياً. الي عنده صلاحية عامة
// اليوم ما راح يفقد شغله الأساسي — بس الشاشات الفرعية تنخفي لحد ما
// المالك ينطيها صراحةً. وهذا مقصود: الافتراضي الأضيق أسلم من
// الافتراضي الأوسع.
func scopedPermissionMigrations() []Migration {
	return []Migration{
		{
			Version: "0242_scoped_permissions",
			SQL: `
				INSERT INTO "Permission" (id, name, label) VALUES
					(gen_random_uuid()::text, 'bookings_archive',
					 'أرشيف الحجوزات — الاطلاع على الحجوزات المؤرشفة'),
					(gen_random_uuid()::text, 'extra_tasks_assign',
					 'توجيه المهام الإضافية — تكليف الموظفين بمهام خارج الحجوزات'),
					(gen_random_uuid()::text, 'training_manage',
					 'إدارة برامج التدريب — إضافة وتعديل البرامج'),
					(gen_random_uuid()::text, 'staff_stats',
					 'إحصائيات الموظفين — الاطلاع على أرقام أداء الكادر')
				ON CONFLICT (name) DO NOTHING;
			`,
		},
	}
}
