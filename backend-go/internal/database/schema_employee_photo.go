package database

// ═══ صورة الموظف ═══
//
// «هنا ضيفلي خاصية أكدر أضيف صورة بدل الحرف — أضيف صورة، ومن أضغط
// عليها تنفتح. أريد أضيف صور للموظفين».
//
// الحرف الأول ينفع كبديل لمن ماكو صورة، بس ما يعرّف: ثلاث موظفين
// اسمهم يبدي بـ«ع» ياخذون نفس الحرف ونفس اللون، والإداري الي يشوف
// كادر حجز ما يميّزهم إلا بقراءة الاسم كامل.
//
// ⚠️ العمود يخزّن **مسار** الصورة مو الصورة نفسها: نظام الملفات
// (`/api/files/...`) موجود ويخزّن على R2 أو القرص، ويتحمّل الأحجام
// والأنواع والتحقق منها. حشر صورة Base64 بعمود نصّي يضخّم كل استعلام
// يجيب موظف — وقوائم الموظفين تنجاب بعشرات الصفوف بكل شاشة.
func employeePhotoMigrations() []Migration {
	return []Migration{
		{
			Version: "0249_employee_photo",
			SQL: `
				ALTER TABLE "Employee"
					ADD COLUMN IF NOT EXISTS "photoUrl" TEXT;
			`,
		},
	}
}
