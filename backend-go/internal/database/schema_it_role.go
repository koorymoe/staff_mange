package database

// ═══ دور «الدعم التقني» (IT) ═══
//
// شغل الـIT بالشركة موجود فعلاً — حاسبات ومخدّمات وشبكة داخلية
// وكاميرات المكتب — بس ما كان له **دور** بالنظام، فالي يشتغل بيه
// ينحط «تقني» أو «فني» وتنمنح له صلاحيات بالإيد فرد-فرد.
//
// ⚠️ **وليش ترحيلان مو واحد**: بوستگرس **ما يسمح** تستعمل قيمة
// enum جديدة بنفس المعاملة الي أضافتها. وكل ترحيل عندنا بمعاملته
// (شوف الحلقة بـmigrate.go) — فالإضافة بترحيل، والي يستعملها
// بترحيل بعده. دمجهم يطيّح الترحيل بخطأ
// «unsafe use of new value of enum type».
//
// ⚠️ والدور **بلا صلاحيات مالية ولا صلاحيات كوادر**: الـIT يشوف
// أجهزته وإحصائياته وبس. ولا يشوف فلوساً ولا رواتب ولا حجوزات.

func itRoleMigrations() []Migration {
	return []Migration{
		{
			// ① القيمة بالـenum — لحالها بمعاملتها.
			Version: "0278_employee_role_it_support",
			SQL: `
				ALTER TYPE "EmployeeRole" ADD VALUE IF NOT EXISTS 'IT_SUPPORT';
			`,
		},
		{
			// ② الصلاحيات الثلاث. وحدة القائمة منفصلة عن الصلاحيتين
			// حتى يكدر المالك يفتح الوحدة لموظف بلا ما ينطيه الجرد،
			// أو ينطيه الجرد بلا ما تبين الوحدة كاملة.
			Version: "0278_it_permissions",
			SQL: `
				INSERT INTO "Permission" (id, name, label) VALUES
					(gen_random_uuid()::text, 'unit_it', 'وحدة تقنية المعلومات'),
					(gen_random_uuid()::text, 'it_assets', 'جرد أجهزة تقنية المعلومات'),
					(gen_random_uuid()::text, 'it_stats', 'إحصائيات تقنية المعلومات')
				ON CONFLICT (name) DO NOTHING;
			`,
		},
		{
			// ③ الأدوار الثانوية.
			//
			// 🔴 **ليش انحتاجت**: بشركة بحجمنا الواحد يسوي أكثر من شغلة —
			// فني بالميدان **و**يتولى حاسبات المكتب. وقبلها الحل الوحيد
			// إن (ع) يمنح صلاحيات IT بالإيد لموظف دوره «فني»، فيصير
			// موظف عنده شاشات ما أحد يعرف **ليش** عنده، ولا يبين
			// بقائمة الموظفين إنه مسؤول الـIT.
			//
			// ⚠️ **والدور الثاني حزمة صلاحيات مو رتبة**: يصبّ صلاحيات
			// دوره الافتراضية وبس. ما يبدّل `role`، وما يمرّ بأي حارس
			// يقرا الدور — فما يكدر يفتح شاشة إدارية بحراسة الدور.
			//
			// 🔴 **وما يمنح OWNER ولا ADMIN أبداً** — مطبَّق بالخادم
			// بـmodel.SanitizeSecondaryRoles، مو بالواجهة: قيد بالواجهة
			// وحدها يُتخطّى بطلب مباشر.
			Version: "0278_employee_secondary_roles",
			SQL: `
				ALTER TABLE "Employee"
					ADD COLUMN IF NOT EXISTS "secondaryRoles" TEXT[] NOT NULL DEFAULT '{}';
			`,
		},
	}
}
