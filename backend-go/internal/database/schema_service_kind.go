package database

// ═══ نوع الخدمة: جي بي اس ولا داش كام ═══
//
// (ع): «من أختار الجي بي اس تطلعلي بس حجوزات الجي بي اس، ومن أختار
// داش كام تطلعلي بس حجوزات الداش كام».
//
// 🔴 **ليش عمود جديد**: الفحص طلّع إن **ماكو بالنظام أي شي يفرّق
// حجز الجي بي اس من حجز الداش كام**. الحجز مربوط بخدمة، والخدمتان
// **مؤشَّرتان بنفس العلامة** (`managerHandlesPaperwork` — «الورق
// على مسؤول الخدمة»). و`ServiceManager` چان يكفي لو كل موظف يشيل
// خدمة وحدة، بس (ع) كال إن أكو موظف يشيل الاثنين — فلازم تأشير
// صريح على الخدمة نفسها.
//
// ⚠️ **وما نخمّنه من اسم الخدمة**: أكو ترحيل قديم يستعمل
// `WHERE name = 'GPS'` فخدمة الجي بي اس معروفة باسمها صراحةً. أما
// اسم خدمة الداش كام **مجهول** وقد يكون عربياً أو منسوخاً بصيغة
// ثانية. والتخمين هنا يخلي **حجوزات تختفي عن الموظف بهدوء** — وهذا
// أخطر من قائمة فيها زيادة. فالجي بي اس ينتأشّر تلقائياً، والباقي
// يأشّره المالك بضغطة من شاشة مسؤولي الخدمات.
//
// ⚠️ **وNULL معناها «ما انتأشّرت»** — وحجوزاتها تطلع بالقائمتين مع
// تنبيه، مو تنخفي. موظف يفتح ويلگى قائمة فاضية يحسب النظام مكسوراً
// ويوقف عن شغله.
//
// ⚠️ **نص مو enum**: إضافة قيمة لـenum ما تنفع بنفس المعاملة الي
// أنشأته — علّة انعضّينا منها قبل. والقيم محصورة بالكود.

func serviceKindMigrations() []Migration {
	return []Migration{
		{
			Version: "0286_service_kind",
			SQL: `
				ALTER TABLE "Service"
					ADD COLUMN IF NOT EXISTS "serviceKind" TEXT;

				UPDATE "Service" SET "serviceKind" = 'GPS'
					WHERE "serviceKind" IS NULL AND name = 'GPS';

				CREATE INDEX IF NOT EXISTS "Service_serviceKind_idx"
					ON "Service" ("serviceKind")
					WHERE "serviceKind" IS NOT NULL;
			`,
		},
		// 0287: تأشير النوع **يشيل الورق عن الفني** لحاله.
		//
		// (ع): «من أسويله تم الإنجاز ميطلب مني فاتورة أو تقرير،
		// المفروض تروح لمسؤول الخدمة هو يسويلها فاتورة».
		//
		// وهاي القاعدة موجودة أصلاً بالنظام وشغّالة — عمود
		// `managerHandlesPaperwork` يقرأه `paperwork_guard.go` (يمنع
		// الفني) و`MyTasks.tsx` (ما يعرضله طلب الورق) وملف الغرامات
		// (ما ينغرَّم عليه). المشكلة إنها **تأشير ثاني منفصل ومطفي
		// افتراضياً**، و(ع) ما يعرف إنها موجودة.
		//
		// 🔴 والحقيقة إن التأشيرين **يعنون نفس الشي** بشغله: الخدمة
		// الي نأشّرها «جي بي اس» أو «داش كام» هي بالتعريف خدمة ورقها
		// على مسؤولها. فالتأشير الواحد صار يسوّي الاثنين، هنا
		// وبمسار التأشير — بلا ما يتعلّم المالك إعدادين.
		{
			Version: "0287_service_kind_implies_paperwork",
			SQL: `
				UPDATE "Service" SET "managerHandlesPaperwork" = true
					WHERE "serviceKind" IS NOT NULL
					  AND "managerHandlesPaperwork" = false;
			`,
		},
	}
}
