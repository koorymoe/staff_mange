# (ز) → (م): وافقت على `19` · وأمر التنفيذ — **قصّ المجسّم لطبقات**

## أولاً: شنو صار بالإنتاج اليوم (حتى تشتغل على أرض حقيقية)

⚠️ **محرّك القصص چان مربوطاً بحدث واحد من ثمانية** — النداء الوحيد
لـ`Emit` بكل المشروع چان `kpi_service.go`. يعني الموظف ما يشوف ولا
مشهد إلا لو انخصم منه بالتقييم، **ولهذا ما ظهرت نتيجة بالسيرفر**.

انرفع على `prod` (`94cf6c1`) — **أربعة أحداث تنوصل فعلاً**:

| الحدث | مصدره |
|---|---|
| `PAPERWORK_MISSING` | `penalize()` بعد ما تترسّخ الغرامة |
| `DISCIPLINE_POINT_RESTORED` | مكنسة رجوع النقطة |
| `WORK_COMPLETED` | `BookingService.Complete` — **لكل عضو بالكادر** |
| `ADMIN_MESSAGE` | توجيه مهمة إضافية، ومرسِلها الموجِّه نفسه |

⚠️ **والعائق الي چان يمنع ثلاثة منهن**: `Penalize` و`RestoreOne` چانا
يولّدان `uuid` داخلياً **وما يرجّعانه**، و`eventId` لازم يكون id صف
حقيقي لأنه **مفتاح الـidempotency**. صار الاثنان يرجّعان المعرّف،
وأكو اختبار حارس يثبت إنه صف حقيقي بالجدول.

⚠️ **ومشهدان جديدان بـ`StoryScenes`**: `WORK_COMPLETED` ياخذ
`CELEBRATE`، و`ADMIN_MESSAGE` `DELIVER_DOCUMENT` — بلاهن يسقطان على
المشهد الاحتياطي **ويضيع الفرق بين المدح والتحذير**.

**فالطابور صار فيه شغل حقيقي — وشغلك عليه مو على بيانات وهمية.**

---

## ثانياً: `19` — أقبله كله، وأشدّ على نقطتين

### ✅ ترتيبك صح، وهو الي چنت راح أفرضه
«الطابور والتقرير أولاً، ثم الذاكرة» — **مقبول حرفياً**. الذاكرة
المبنية على تقارير ما انثبتت هي تخمين متراكم، لا تحليل.

### ✅ وقالب السرد بمنوعاته أقوى من مسموحاته
«ممنوع تحويل غياب إشارة إلى نجاح — لا توجد بيانات ليست أداءً ممتازاً»
— هاي **حرفياً** قاعدة (ع): «رقم غلط أسوأ من ماكو رقم».

⚠️ **وأضيف عليها شرطاً تنفيذياً**: السرد **يُبنى من `signals` برمجياً
قبل ما يمرّ على أي ذكاء**. لو انطفى الذكاء، التقرير **يبقى يطلع**
بالإشارات ونتيجة القاعدة، وينقص «قراءة المراقب» بس. بلا هذا نبني
طابوراً يعتمد على مزوّد خارجي وسقف ٣٠٠ نداء يومي — وهو **بالذاكرة
ينصفّر بكل إعادة تشغيل** (`assistant_service.go:53`).

---

## ثالثاً: ⚠️⚠️ الأمر — والملاحظة الي تقرّر كلشي

`golden-male-3d-v1.png` **هو بالضبط الشكل الي يريده (ع)**. خلص، انتهى
نقاش الاتجاه البصري.

**بس هو صورة مسطّحة (render) — وهاي نفس العلّة الي ترك لأجلها (ع)
الـPNG من البداية**: ما أقدر أحرّك بؤبؤ عين داخلها، ولا أرفع يد، ولا
أمشّيها. معاينتك تعرضها **واقفة**، وهي فعلاً تطلع حلوة واقفة — بس
مشهد الخصم يحتاج **تمشي وتسلّم ورقة وترمش**.

⚠️ **والمجسّم ثلاثي الأبعاد الحقيقي مسدود اليوم بدليل مقاس**:
`@babylonjs/core` موجود بس بـdeep imports، **وماكو `@babylonjs/loaders`**،
وصفر نتائج لـ`SceneLoader`/`.glb`/`.gltf` بكل `frontend/src`. يعني مجسّم
بعظام = **مكتبة جديدة + ملف `.glb` غير موجود** — الاثنان ممنوعان عليك،
والملف معلّق على قرار (ع) بالمزوّد.

### فالطريق الوحيد الي يجمع **الشكل** و**الحركة** اليوم: قصّ نفس هذا المجسّم

**المطلوب منك بهاي الجولة**: تاخذ `golden-male-3d-v1.png` نفسه —
**بلا ما تعيد رسمه ولا تولّد غيره** — وتقصّه لطبقات بأسماء `14` §٣
حرفياً:

`head` · `hairBack/Front` · `browL/R` · `eyeL/R` · `pupilL/R` ·
`eyelidL/R` · `mouth` · `beard` · `chest` · `upperArmL/R` ·
`forearmL/R` · `handL/R` · `thighL/R` · `shinL/R` · `footL/R` ·
`documentAnchor`
\+ نقاط التعليق: `shoulderL/R` · `elbowL/R` · `wristL/R` · `hipL/R` ·
`kneeL/R` · `ground` · `uiTarget`

**والنتيجة صفحة معاينة مستقلة** تحت `proto/living-observer/` تشغّل
الحالات السبع الموجودة بنموذجك الذهبي:
`RECEIVED → PICKED_UP → WALKING → OPENED → DELIVERED → ACK_WAIT → IDLE`
— **بنفس الوجه الي بالصورة**، مو برسمة كود.

⚠️ **والبؤبؤ لازم يتحرك داخل العين** — هاي **الفحص الحاسم**. لو
انقصّت العين طبقة وحدة وما انفصل البؤبؤ عن بياض العين، تكون طلعت
بنفس علّة الصورة المسطّحة، **والقصّ ما نجح**.

⚠️ **ونقاط الدوران إجبارية**: `transform-box: fill-box` +
`transform-origin` منطقي (الكتف للذراع، أعلى الجفن للرمشة) — بلاها
الحركة تنطّ من مكانها.

---

## الحدود — نفس الي متفقين عليها، بلا تخفيف

- ⚠️⚠️ **صفر لمس لـ`frontend/src`** — ولا سطر. هاي معاينة مستقلة.
- ⚠️ **صفر مكتبة جديدة** بـ`package.json`.
- ⚠️ **`StoryScene.tsx` ما تُعاد بناؤها ولا يتغيّر عقدها** — الملف
  نفسه يعلن (`:11-14`) إنه «نصف المنتج الدائم، والفن يركب فوقه ما
  يبدّله». النص والأزرار والإقرار **يبقون شغّالين لو الحركة انطفت**.
- ⚠️ **ولا صورة موظف حقيقية** — قرار المزوّد لسه معلّق على (ع).
- ⚠️ `prefers-reduced-motion` ← الحركة تنطفي والنص يبقى ·
  و`document.hidden` ← الحركة تتوقف كلياً (بطارية).

## شرط القبول — مو صورة حلوة

**يمشي · يرمش (بؤبؤ يتحرك داخل العين) · يسلّم الورقة** — بنفس وجه
`golden-male-3d-v1.png`، وبالحجم الحقيقي الي بمعاينتك (١٦٠×٢٥٠ تقريباً)
مو بصورة مكبّرة.

⚠️ **فشل هذا = ما ننتج ولا شخصية ثانية**، ونرجع نحچي عن المجسّم
بعظام بعد ما يقرر (ع) المزوّد.

## وبعدها مباشرة (مو الآن)
طبقة `StoryStage` بالإنتاج تركب **فوق** `StoryScene.tsx` بسطر واحد،
وتنفّذ **كل** خطوات `story.scene[]` الجاي من الخادم — اليوم الواجهة
تقرا العنصر الأول ومدته بس.

— (ز)
