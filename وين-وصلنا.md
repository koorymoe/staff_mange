# وين وصلنا — نقطة الاستئناف

> آخر تحديث: ٢٠٢٦-٠٨-٠٩ · الفرع: `prod` و`claude/zen-mendel-70b9j2`
> آخر كوميت: المهام الإضافية (0245)
>
> **هذا الملف نقطة الوصل بين الجلسات.** كل جلسة جديدة تبدي من هنا:
> تقرا «الباقي» وتكمل، وتحدّث الملف قبل ما تخلص.

---

## ✅ الي خلص وانرفع

| الكوميت | شنو |
|---|---|
| `6e64382` | هوية كاملة بكل مكان + إصلاح كود الزبون المكسور |
| `2e5aa4c` | سجل تعديلات فاتورة الليدر — تعديل المحاسب كان **يمحي** الأرقام الأصلية |
| `49f4855` | ربط الهوية: صندوق المراقب، التنسيق، التقارير، وطباعة فاتورة الليدر |
| `f1a2b75` | فتح الحسابات وبيانات الدخول للمالك وحده + شاشة رمز مركز القيادة |
| `79947ee` | الستارة — ترتفع من ركن الشاشة وتكشف دخول مركز القيادة |
| `596920b` | مركز القيادة: الشعاع المنزلق + توسيع لـ٦ أقسام و١٦ تبويب |
| — | مراحل الحجز: منو رحّله · ست سلال · ملاحظات موجّهة · الليدر يشوف حجزه · حجز غير مثبّت ما يطلع بجدول يوم |
| — | **نواة الذكاء الاصطناعي (0243)** + رقم الفاتورة للمراقب + الحجز غير المنسّق ما يطلع بالقائمة |
| — | الحقول الإجبارية (0244): بيان صرف الدوار + تفاصيل أجهزة الجي بي اس · وتوحيد ٦ تسميات متضاربة |
| (الأخير) | **المهام الإضافية (0245)** — المدير يوجّه شغل لموظف، مو مربوط بحجز |

### تفاصيل مختصرة

**الهوية (الدفعة ٥)** — `EntityIdentity` + `utils/identity.ts` + `utils/printIdentity.ts`.
مركّبة بـ: التنسيق، الحجوزات الجزئية، تقارير العمل، مراجعة التقارير، متابعة
الجودة، فاتورة جديدة، قائمة الفواتير وتفاصيلها، صندوق المراقب.
هوية صفوف المراقب **تنبني بالسيرفر** (`MonitorIdentity` بـ`monitor_review_repository.go`):
استعلامين للصفحة كلها مو استعلام لكل صف. النوع الي ما إله حجز يبقى بلا هوية.

**طباعة فاتورة الليدر** — ما كان بيها طباعة أصلاً. انضافت برأس الهوية
وسجل التعديلات، وكل نص يمرّ بـ`esc()`.

**الحسابات** — `POST /api/employees` وتغيير `username`/`password` بـ
`PUT /api/employees/{id}` صارت للمالك وحده. حارس جديد `RequireOwnerOnly`
يرجّع 403 **بلا تسجيل مخالفة** (RequireRole چان يحظر حساب مدير النظام
بعد ٣ ضغطات على زر باقي بالكاش).

**مركز القيادة** — طبقة PPSK: نفس اليوزر، رمز ثاني.
الرمز يتحط من شاشة «🔐 رمز مركز القيادة» (المالك وحده، والمسار يرجّع 404 لغيره).
الستارة بشاشة الدخول تكشف الواجهة الحمراء — **بس الرمز هو الي يقرر النظام
مو الشاشة**، والأربع حالات مفحوصة.
الشاشات **فارغة قصداً** — ماكو ولا معلومة تنتقل من نظام الشركة.

---

## ⏭️ الباقي — من هنا نكمل

### الدفعة ٦ — الخط الزمني والتأخيرات
مسار `GET /api/bookings/{id}/timeline` يجمع أحداث الحجز كلها بالترتيب.

يحتاج **٣ دوال مستودع جديدة**:
- `MonitorReviewRepository.ByEntity(entityType, entityID)`
  ⚠️ صفوف الفواتير مفتاحها **معرّف الفاتورة** مو الحجز — لازم نجيب فواتير الحجز أول.
- `QualityFollowUpRepository.ByBooking(bookingID)` — سهل، `bookingId` عليه قيد فريد.
- `LeaderInvoiceRepository.ListByBooking(bookingID)` — `bookingId` ما ينستعمل بأي WHERE اليوم.

موجود أصلاً: `booking_repository.go ScheduleLog()` و`booking_progress_repository.go Reports()`.
**ماكو بيانات جديدة تنجمع** — كلها موجودة ومتفرقة.

**التأخيرات الستة** (تنحسب من أوقات موجودة، بلا أعمدة جديدة):

| المقياس | الحساب | مسؤولية منو |
|---|---|---|
| تأخر التثبيت | `confirmedAt − createdAt` | إداري الحجوزات |
| تأخر التكليف | أول تعيين − `confirmedAt` | إداري الحجوزات |
| تأخر الخروج | `startedAt − scheduledAt` | الفني/الليدر |
| مدة التنفيذ | `completedAt − startedAt` | الفني/الليدر |
| تأخر الفوترة | إنشاء الفاتورة − `completedAt` | الليدر |
| تأخر التدقيق | `approvedAt −` إنشاء الفاتورة | المحاسب |

⚠️ الحد يكون **ثابت معلن** مثل غرامة الـ٣٦ ساعة الموجودة، مو رقم مخفي.
⚠️ **عرض بس** — ماكو غرامة تلقائية بهذي المرحلة.
⚠️ الحقل الفارغ ينختفي: حجز ما وصل الفوترة **ما يعرض** «تأخر الفوترة: —».

### الدفعة ٧ — وثيقة الأدوار والصلاحيات
صفحة «📋 دليل الأدوار» (المالك ومدير النظام) + نسخة Markdown بالمستودع.
تتولّد من ٣ مصادر حقيقية حتى ما تكذب: جدول `Permission`، شجرة `navItems`،
وحراس المسارات. **تعيد استعمال `isNavVisible()`** — نفس مصدر «شوف بعين الموظف».
لكل دور: شنو يشوف، شنو يكدر، **شنو ما يكدر**.

### الذكاء الاصطناعي — الي انبنى والي باقي
**انبنى:** `AiSignal` / `AiEvidence` / `AiVerdict` / `AiMetric` / `AiWorkWindow`،
جامع أدلة توقف العمل كامل، محرّك قواعد `RulesJudge` بأربع قواعد + كشف النمط،
شاشة «🧠 مؤشرات الذكاء الاصطناعي» (ADMIN/OWNER بس)، ومسارات `/api/ai/*`.

**باقي:**
- جوامع أدلة لبقية الإشارات (تأخر الخروج، التأجيل المتكرر، تعديل الفاتورة،
  الإنجاز الجزئي) — الهيكل جاهز، ينضاف جامع لكل نوع.
- **حاسبة المؤشرات**: جدول `AiMetric` موجود وفاضي — تنحسب من الأدلة المتراكمة.
- **ربط المنصّة**: `service.Judge` واجهة، و`AiBrainService.SetJudge()` نقطة
  التوصيل الوحيدة. تنفيذ `Judge` ثاني ينادي المنصّة ويرجّع نفس `AiVerdict`.
  ⚠️ ما ينلمس جامع الأدلة أبداً — الأدلة تبقى تنجمع عدنا.

### ⏸️ طلبات وصلت وما انبنن بعد
- **فاتورة الشبكات ضمن فاتورتنا** — «مو عدنا كاميرات IP وهاي الترتيب،
  أريدها تظهر هناك». محرّك كلفة التنفيذ يشتغل على `SystemPriceCatalog`
  (سعر × عدد × مضاعفات)، أما الشبكات فتسعيرتها بثلاث أنماط
  (FLAT/TIERED/BRACKET) بجدول `NetworkPriceItem` ومحرّك `CalculateLine`.
  ⚠️ الدمج يمس **حساب الفلوس**، فلازم: بنود شبكات بطلب الفاتورة، حسابها
  بمحرّكها هي، تخزين تفصيلها، وفحص إن المجموع الصافي ما ينكسر بالفواتير
  القديمة.
- ~~ترتيب الواجهات~~ ✅ انوحّدت التسميات الست. الباقي (٣٧ مسار مكرر
  بنفس التسمية) **مقصود**: نفس الشاشة لأدوار مختلفة.

### الحقول الإجبارية — «ريكوست فيلد»
**انبنى:** بيان صرف الدوار إجباري (حجز مربوط أو نص ≥٥ حروف)، وتفاصيل
أجهزة الجي بي اس (عدد + نوع مركبة). الاثنين مفروضين **بالسيرفر** مو
بالواجهة بس.

⚠️ **الآلية قابلة للتوسّع بلا كود**: عمود `Service.requiresDeviceInfo` —
صاحب العمل يأشّر أي خدمة ثانية وتصير تطلب نفس الحقول.

**باقي** (صاحب العمل گال «أكو هواي أشياء هيج»): ينتظر منه قائمة أي
شاشات ثانية تحتاج حقول إجبارية.

### المهام الإضافية ✅
جدول `ExtraTask` مستقل عن الحجوزات (الحجز إله زبون وموعد وفاتورة —
والمهمة ما إلها ولا وحدة، وحشرها بالحجوزات يلوّث كل إحصائية تعدّ
الحجوزات). شاشة `/extra-tasks` للمدير (توجيه ومتابعة) و`MyExtraTasks`
بشاشة «مهامي» للموظف. إشعار عند التوجيه والإنجاز والإلغاء.

⚠️ **ملكية المهمة تنفحص بالمستودع بكل عملية** (`assignedToId` بالـWHERE)
مو بالحارس: الحارس يعرف إنه موظف، ما يعرف إنها **مهمته هو**.

### مؤجّلات بطلب صاحب العمل
- خانات الكادر غير المحدودة + قرار عمولة الفنيين — «اتركهن هسه».
- الطاقة الشمسية لسه ما انربطت بصندوق المراقب.

---

## 🔑 على صاحب العمل (مو علينا)

1. **حط رمز مركز القيادة** من «🔐 رمز مركز القيادة» بالقائمة الجانبية.
   ⚠️ ما ننختار الرمز عنه ولا ينخزن عندنا.
2. **شغّل النسخ الاحتياطية**: `./setup-backups.sh` بالسيرفر بـ`BACKUP_PASSPHRASE`
   مالته هو. التذكير يشتغل كل ٤ ساعات.

---

## 🧪 بيئة الفحص المحلية

```bash
(sudo dockerd &) ; sleep 6 ; docker start pg
kill $(pgrep -f "go-build.*/api$")          # ⚠️ بلا هذا تخدم كود قديم
cd backend-go && OWNER_USERNAME=owner OWNER_PASSWORD='Test12345!' \
  CORS_ORIGIN='http://localhost:5173,http://localhost:5199,http://localhost:5200' \
  DATABASE_URL='postgres://staffmange:localpass@localhost:5432/staffmange?sslmode=disable' \
  go run ./cmd/api
cd frontend && npm run dev -- --port 5200   # الأساس /staff_mange/
```

قاعدة البيانات: المستخدم والقاعدة `staffmange`، الباسورد `localpass`.
المتصفح للفحص: `/opt/pw-browsers/chromium`.

**قبل أي كوميت:** `go build ./...` + `npx tsc --noEmit` + `npm run build`.

### مطبّات انلدغنا بيها قبل
- عمود بقاعدة البيانات بلا حقل مقابل بـGo **يكسر** `SELECT *`.
- `len()` بـGo يعد **بايتات مو حروف** — العربي يحتاج `utf8.RuneCountInString`.
- `Number('')` بجافاسكربت = **صفر**، مو فاضي.
- `PUT /api/permissions/employee/{id}` **يستبدل** القائمة كلها، مو يضيف عليها.
- رموز ثيم مركز القيادة محصورة بـ`.cmd-root` — أي عنصر برّاه يحتاج
  إعادة إعلانها، وإلا `background` كله يصير لاغي (= شفاف).
