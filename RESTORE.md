# النسخ الاحتياطي والاسترجاع — نظام شركة الأماني

## بأربع أوامر

```bash
cd ~/staff_mange

./setup-backups.sh     # مرة وحدة بس — يفعّل النسخ التلقائي اليومي
./backup-status.sh     # فحص: هل النسخ شغالة فعلاً؟ (شغّله كل أسبوع)
./backup-db.sh         # نسخة يدوية الحين (قبل أي تعديل كبير)
./restore-backup.sh    # استرجاع من آخر نسخة
```

---

## شنو ينحفظ بالنسخة

كل نسخة فيها **ثلاث** أشياء — وكلها ضرورية:

| الجزء | شنو بيه | ليش ضروري |
|---|---|---|
| `database.sql` | الحجوزات، الزبائن، الموظفين، الفواتير، المنظومات، الغرامات — كل شي | بدونه ماكو نظام |
| `uploads.tar` | صور المنتجات، الوصولات، وثائق السيارات | بدونه النظام يشتغل بس كل صورة تطلع مكسورة |
| `env.txt` | كلمات السر، `JWT_SECRET`، بيانات المالك | **بدونه ما تكدر تشغّل النظام أصلاً** — كلمة سر قاعدة البيانات بيه |

> ⚠️ `JWT_SECRET` تحديداً: لو ضاع وتغيّر، كل الموظفين ينطردون من جلساتهم فوراً ولازم يسجّلون دخول من جديد.

**النسخ تنحفظ ١٤ يوم** ثم تنحذف الأقدم تلقائياً حتى ما تمتلئ مساحة القرص.

---

## ⚠️ أهم شي بهذا الملف: النسخة خارج السيرفر

النسخ الي بمجلد `backups/` موجودة **على نفس السيرفر**.

يعني لو تلف السيرفر أو انحذف أو ما عاد عندك وصول له — **تروح كل النسخ معاه بنفس اللحظة**. عملياً ما عندك نسخة احتياطية، عندك نسخة ثانية من نفس الشي بنفس المكان.

**لازم تفعّل واحد على الأقل من هذولا:**

### الخيار ١: Cloudflare R2 — يومي وتلقائي
مجاني لحد ١٠ جيجا. النسخة تنرفع تلقائياً بعد كل نسخة يومية.

```
R2_BUCKET=اسم-الباكت
R2_ACCESS_KEY=...
R2_SECRET_KEY=...
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
```
ثم: `apt-get install -y awscli` وجرّب `./backup-db.sh`

### الخيار ٢: Hetzner Backups — صورة كاملة للسيرفر
`Server → Options → Backups → Enable` (٢٠٪ من سعر السيرفر).
تاخذ صورة كاملة أسبوعياً — تحميك من ضياع السيرفر، بس **أسبوعية**، يعني ممكن تخسر شغل أسبوع.

**الأفضل: فعّل الاثنين.** R2 يومي + Hetzner أسبوعي.

---

## الاسترجاع

### ضاعت بيانات (قاعدة البيانات تخربت أو انمسح شي بالغلط)

```bash
cd ~/staff_mange
ls -lh backups/                    # شوف النسخ المتوفرة
./restore-backup.sh                # آخر نسخة
./restore-backup.sh backups/staffmange_20260807_030000.tar.gz   # نسخة محددة
```

السكربت **ياخذ نسخة أمان من الوضع الحالي أول** قبل ما يسترجع — فلو استرجعت النسخة الغلط تكدر ترجع.
ويسألك «متأكد؟» ولازم تكتب **نعم** حتى ينفّذ.

بعد الاسترجاع: `docker compose restart backend`

### السيرفر كله راح

1. سيرفر جديد + نصّب Docker
2. `git clone` المشروع
3. نزّل آخر نسخة من R2 (أو من صورة Hetzner)
4. حط ملف `.env` — من `env.txt` الي جوّا النسخة
5. `docker compose up -d`
6. `./restore-backup.sh backups/آخر-نسخة.tar.gz`
7. وجّه الدومين على الـIP الجديد

---

## متى تسوي نسخة يدوية

- **قبل أي `docker compose up -d --build`** — يعني قبل كل نشر
- قبل أي تعديل كبير على البيانات (استيراد، حذف جماعي)
- قبل ما تلعب بإعدادات السيرفر

```bash
./backup-db.sh
```

---

## اطمّن كل أسبوع

```bash
./backup-status.sh
```

يجاوبك بصراحة: الجدولة شغالة لو لا، آخر نسخة متى صارت، شنو جوّاها فعلاً، واكو نسخة خارج السيرفر لو لا.

> أخطر شي بالنسخ الاحتياطي مو إنك ما عندك نسخ — **إنك تظن عندك نسخ وهي واقفة من شهرين**. لهذا السبب موجود هذا الأمر.

---

## اختبر الاسترجاع مرة بالسنة

نسخة ما جرّبت ترجّعها = نسخة ما تعرف إذا تشتغل.

على سيرفر تجربة (مو الإنتاج): نزّل نسخة، استرجعها، سجّل دخول، وتأكد الحجوزات والصور موجودة.
