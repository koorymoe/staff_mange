# استرجاع النظام بعد عطل — دليل سريع

عدنا خطين دفاع منفصلين، استخدم الأنسب حسب الحالة:

## 1) فقدان بيانات فقط (قاعدة البيانات تخربت أو انمسحت بالغلط)
النسخ الاحتياطية اليومية موجودة بمجلد `backups/` على السيرفر نفسه (تلقائية كل يوم 3 فجراً، آخر 14 يوم محفوظين).

```bash
cd ~/staff_mange
ls -lh backups/                      # شوف آخر نسخة متوفرة
gunzip -c backups/staffmange_TIMESTAMP.sql.gz | docker exec -i staff_mange-db-1 psql -U staffmange -d staffmange
```

⚠️ هذا الأمر يستبدل البيانات الحالية بالكامل بالنسخة المحفوظة — استخدمه بس وقت الحاجة الفعلية.

## 2) عطل كامل بالسيرفر نفسه (السيرفر مو شغال أو تلف كلياً)
1. فعّل ميزة "Backups" الرسمية من Hetzner (Server → Options → Backups → Enable) — تاخذ صورة كاملة أسبوعية للسيرفر كامل تلقائياً.
2. من لوحة Hetzner: أنشئ سيرفر جديد من آخر Backup/Snapshot محفوظة.
3. بمجرد ما السيرفر الجديد يشتغل، استرجع آخر نسخة من قاعدة البيانات (خطوة 1 أعلاه) لو كانت أحدث من الـ Snapshot.
4. حدث DNS/تعامل مع تغيير الـ IP إذا صار (أو استخدم "Floating IP" من Hetzner لتفادي تغيير العنوان أصلاً — راجع هذا لاحقاً لو الشركة كبرت).

## تفعيل النسخ الاحتياطي التلقائي (مرة وحدة بس، أول مرة)
```bash
cd ~/staff_mange
./setup-backups.sh
```
