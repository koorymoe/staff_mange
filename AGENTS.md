# شغل هذا المستودع

## ═══ الفروع ودفع الشغل ═══

**`avg` هو فرع الشغل المشترك** (١٦ أيلول ٢٠٢٦) — الفريق كله يشتغل
عليه. انبنى نسخة طبق الأصل من `prod` وقتها (`8e0fb03`).

**`prod` هو الي ينشتغل عليه فعلاً بالسيرفر.** صاحب النظام يسحب منه
(`git pull origin prod`) وبعدها يبني.

🔴 **وهاي أهم سطر بالملف**: من يوم ما انتقلنا لـ`avg`، أي شغل
ينخلص **ما يظهر على السيرفر** لحد ما ينترقّى. فلو سحب صاحب النظام
وما جاه شي — **هذا صحيح مو خلل**. والترقية تصير بعد ما نكمّل
ونتأكد، بطلبه نصاً: «بعد ما نكمل كلشي ونتأكد من صحة عملنا نرفع
على برود».

**الترقية:**
```
git push origin avg:prod          # fast-forward، بعد الفحص
```

**فرع التاريخ:** `claude/zen-mendel-70b9j2` — كل النظام مبني عليه
(٥٨٨ رفعة من ١١ حزيران ٢٠٢٦). يبقى مرجعاً، ما ننشره ولا نشتغل
عليه بعد هسه.

**`main` فاضي** — واقف على «Initial commit» ولا أحد يستعمله. لا
تنشره ولا تعتمد عليه كأساس.

### الطريقة المتفق عليها

بعد كل شغلة تخلص وتنفحص:

```
git push -u origin avg            # فرع الشغل المشترك
```
والترقية لـ`prod` **خطوة منفصلة** تصير بعد ما يكمل الشغل وينفحص —
مو مع كل دفعة.

⚠️ **انتبه — القاعدة انتبدّلت (١٦ أيلول ٢٠٢٦)**: چان الدفع لـ`prod`
مباشرة بعد كل تعديل بإذن صريح (١٨ آب): «اي ادفع على برود مباشرة».
وهسه صاحب النظام طلب العكس: الشغل يتجمّع على `avg` ويُرقّى **بعد**
ما يكمل وينتأكد. فالدفع المباشر لـ`prod` **وقف**.
بس **سبب** الإذن القديم يبقى صحيحاً ولازم ننتبهه: إنه يسحب وما
يجيه شي ويظن الشغل ضايع. فكل جواب يخلص لازم يذكّر إن الشغل على
`avg` وما وصل السيرفر بعد.

⚠️ **بس بشرط:** ما ينداز شي على `prod` إلا بعد ما ينجح
`npx tsc --noEmit -p tsconfig.app.json` و`npx eslint src` و
`npm run build` — وينفحص بمتصفح حقيقي على سيرفر شغّال، مو بالبناء
لحاله. هذا سيرفر إنتاج، والي ينكسر بيه يوقف موظفين عن شغلهم.

⚠️ **`prod` لازم يبقى fast-forward** من `avg`. لو تقدّم
لحاله (صاحب النظام يشتغل عليه من واجهة GitHub أحياناً — مثل شغل
شاشة الدخول)، **ادمجه بفرع التطوير أول** قبل أي دفع، وإلا شغله
ينمسح.

### التذكير للموظف بعد الدفع

أي تعديل بالواجهة ما ينشاف بلا `npm run build` على السيرفر —
اذكرها بالجواب.

## ═══ الفريق ومساراته ═══

من ١٦ أيلول ٢٠٢٦ صار الفريق **أربعة**، وهذا توزيع صاحب النظام
نصاً: «انت الباك اند والاساس والافرع · م يمه التصميم والترتيبات
والذوق والديكور · والعميل الجديد مسؤول ع النظام وع الذكاء
الاصطناعي الي راح يتحرك بحرية بداخل النظام».

| منو | مساره | ما يلمسه |
|---|---|---|
| **(ع) المالك** | القرار والأولويات · إعدادات گيتهب · النشر على السيرفر | — |
| **(ز) Claude** | الباك إند · الأساس · الفروع والدمج · الفحص الحيّ قبل الترقية | — |
| **(م) Codex** | التصميم والترتيبات والذوق والديكور | منطق الباك إند · الترحيلات |
| **الوكيل الجديد** | النظام والذكاء الاصطناعي — **يتحرك بحرية داخل النظام** | يرقّي `prod` بلا فحص |

🔴 **وأي أحد يحتاج مساعدة من الثاني يطلبها بلا أي إحراج** — طلب
صاحب النظام حرفياً: «كلكم تشتغلون سوه واي احد يحتاج مساعده من
الثاني يطلبها بدون اي احراج». مكتوبة هنا بقصد، لأن الذاكرة تنتهي
مع الجلسة والملف يبقى.

⚠️ **والمسارات حدود شغل مو حدود حديد**: «بس شي مربوط بشي» — الي
يلمس ملفاً برّا مساره يذكرها بجوابه، ويسأل صاحبه إذا الشغل يتقاطع.

### الأساس الموجود للذكاء الاصطناعي — ابنِ عليه، لا تبنيه من الصفر

- `backend-go/internal/service/ai_brain_service.go` ·
  `ai_evidence_service.go` · `ai_metrics_service.go`
- `backend-go/internal/repository/ai_repository.go`
- `backend-go/internal/model/ai.go`
- `backend-go/internal/database/schema_ai_core.go`
- المفاتيح بالبيئة (`internal/config/config.go:47-51`):
  `GEMINI_API_KEY` · `GEMINI_DAILY_CAP` (٣٠٠ افتراضياً) ·
  `GEMINI_IMAGE_MODEL`

🔴 **قيد قائم على الذكاء الاصطناعي — والوكيل الجديد أول من يصطدم
بيه**: **صور الموظفين ما تنداز لأي مزوّد خارجي** قبل قرار صاحب
النظام **المكتوب** على أربع نقاط: الخصوصية · الكلفة · محل المعالجة ·
مدة الحفظ. لحد الآن ماكو قرار — فالميزات الي تعتمد على تحليل الصور
(المشاعر والتحليل العميق) **موقوفة**، مو منسية.

🔴 **والقاعدة الي ما تنكسر**: الغرامات والنقاط **بالقواعد حصراً**،
وكل غرامة بسبب ودليل. المشاعر تقرّر **شلون يتكلم** النظام، مو
**منو ينغرم**.

## ═══ قرار بيانات الزبائن (١٦ أيلول ٢٠٢٦) ═══

المستودع چان **عاماً** وفيه:
- `backend-go/cmd/importgps/gps_data.json` — **٥٥٤ سجل زبون**:
  الاسم الكامل · الهاتف · رقم الجي بي اس · IMEI · انتهاء الاشتراك ·
  اسم المنصِّب · كلفة التنصيب · نوع المركبة.
- `backend-go/data/uploads/misc/*.png` — أربع صور (المجلد نفسه
  مستثنى بـ`.gitignore` هسه، يعني انرفعن **قبل** الاستثناء).

**قرار صاحب النظام**: يخلّي المستودع **خاصاً (Private)** بنفسه من
إعدادات گيتهب، و**الملف والصور يبقون** — قرار صريح مقصود، مو سهو.

⚠️ ويبقى معروفاً: هذولا **بالتاريخ**، فمن يعرف الكومِت يوصلهم حتى
بعد أي شيل لاحق — محوهم نهائياً يحتاج إعادة كتابة تاريخ (دفع قسري،
وكل الفريق يعيد جلب المستودع).
⚠️ وتُفترض البيانات **منكشفة فعلاً** لأنها كانت عامة مدة — أي نسخة
انأخذت قبل القفل ما ترجع.

## ═══ التشغيل المحلي بحاوية (الي يشوفه صاحب النظام) ═══

سؤال صاحب النظام: «إذا نشغله لوكل شنو الخطوات بالفي اس كود؟»

```
git fetch origin && git checkout avg
cp .env.production.example .env
#   DB_PASSWORD=localpass
#   JWT_SECRET=local-dev-secret-key-32chars-min!!
#   CORS_ORIGIN=http://localhost:8081
#   DOMAIN=localhost
#   OWNER_USERNAME=…   OWNER_PASSWORD=…
docker compose up -d --build db backend frontend
# والموقع: http://localhost:8081
```

⚠️ **بلا `caddy`** — لاحظ إن الخدمات محدَّدة بالأمر. Caddy تحاول
تجيب شهادة HTTPS لدومين حقيقي وتاخذ المنفذين ٨٠ و٤٤٣، فمحلياً تفشل
وتدور بحلقة إعادة تشغيل. والفرونت مربوط على `127.0.0.1:8081`
مستقلاً عنها (`docker-compose.yml`).

⚠️ **وماكو `/staff_mange/` محلياً**: `frontend/Dockerfile` يبني
بـ`VITE_BASE_PATH=/` و`VITE_API_URL=/api` (وnginx يوكّل `/api`
للباك إند بنفس المنفذ) — فالرابط جذر مباشرة.

⚠️ **`CORS_ORIGIN` لازم يطابق المنفذ** — لو غلط، الدخول يفشل
بـ«Failed to fetch» ويطلع النظام مكسوراً وهو سليم.

⚠️ **`.env` ما ينرفع أبداً** — بيه كلمات سر.

⚠️ **وتغيير الفرع ≠ تشغيل محلي**: الاثنان منفصلان. الشغل على `avg`
ما يمس السيرفر أبداً، والتشغيل المحلي **اختياري** — بديله إن (ز)
يقيس ويوري النتائج، أو نسوي نسخة staging على السيرفر تسحب من `avg`
بمنفذ ثاني.

## ═══ التشغيل المحلي للفحص (بلا حاوية — للوكلاء) ═══

```
dockerd &                                    # لو مو شغّال
docker start pg                              # المستخدم staffmange / localpass
cd backend-go && DATABASE_URL='postgres://staffmange:localpass@localhost:5432/staffmange?sslmode=disable' \
  JWT_SECRET='local-dev-secret-key-32chars-min!!' PORT=4000 CORS_ORIGIN='http://localhost:5199' \
  go run ./cmd/api
cd frontend && npx vite --port 5199
```

⚠️ `CORS_ORIGIN` لازم يطابق منفذ Vite، وإلا الدخول يفشل بـ
«Failed to fetch» والفحص يطلع إيجابية كاذبة.
⚠️ `JWT_SECRET` إجباري (١٦ محرفاً فأكثر) — بدونه السيرفر يطلع.
⚠️ متصفح Playwright: `/opt/pw-browsers/chromium` — لا تنزّل غيره.

## ═══ دروس انكوينا بيها ═══

**الوضع الليلي بـ`frontend/src/index.css`:** القلب مركزي (متغيّرات
+ أصناف) مو `dark:` بكل شاشة. ⚠️ نفس المتغيّر يخدم النص والخلفية
(`--color-red-600` تخدم `text-red-600` و`bg-red-600`) — فالقلب
بالمتغيّر يصلّح وحدة ويكسر الثانية. اقرا التعليقات هناك قبل ما
تلمسه، وقيس التباين بالمتصفح مو بالعين.

**عائلة `gray` غير `slate`** بـTailwind — واثنينهن مستعملات.
نسيان وحدة يخلّي نص شاشات ما ينقرا.

**صيغة الشفافية صنف مستقل:** `.bg-sky-50\/70` ما تنصاد بقاعدة على
`.bg-sky-50`. الي ينستعمل بشفافية بكثرة لازم ينقلب بالمتغيّر.

**المناطق الغامقة بالوضعين** (القائمة الجانبية `.app-sidebar`)
تنستثنى من قلب الرمادي — وإلا تنعمى.

**جلب البيانات:** الجلب بمكان واحد داخل `useEffect` مع `alive`
تمنع سباق الطلبات، والتحديث اليدوي يطلب برفع عدّاد
(`setReload(n => n + 1)`) مو بنداء دالة تحدّث الحالة من جسم
الـeffect. تبديل فلتر بسرعة بلا هاي الحماية يخلّي جواب قديم يطمس
الجديد — والموظف يتخذ قرار على بيانات غلط.

**دور المالك ينقلب بالجلسة:** `role: 'OWNER'` تصير `'ADMIN'`
والدور الحقيقي بـ`actualRole`. أي فحص للمالك لازم يكون
`actualRole === 'OWNER'` — `role === 'OWNER'` ما تتحقق أبداً.
