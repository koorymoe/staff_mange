# شغل هذا المستودع

## ═══ الفروع ودفع الشغل ═══

**`prod` هو الفرع الي ينشتغل عليه فعلاً.** صاحب النظام يسحب منه
على السيرفر بعد كل تعديل (`git pull origin prod`)، وبعدها يبني.

**فرع التطوير:** `claude/zen-mendel-70b9j2` — كل النظام مبني عليه
(٥٨٨ رفعة من ١١ حزيران ٢٠٢٦)، و`prod` مبني فوگه.

**`main` فاضي** — واقف على «Initial commit» ولا أحد يستعمله. لا
تنشره ولا تعتمد عليه كأساس.

### الطريقة المتفق عليها

بعد كل شغلة تخلص وتنفحص:

```
git push -u origin claude/zen-mendel-70b9j2     # فرع التطوير
git push origin claude/zen-mendel-70b9j2:prod   # وبعده prod مباشرة
```

⚠️ **الدفع لـ`prod` مو محتاج إذناً جديداً كل مرة** — صاحب النظام
أذن بيه صراحةً (١٨ آب ٢٠٢٦): «اي ادفع على برود مباشرة بعد كل
تعديل». الانتظار حتى يجي الإذن يعني إنه يسحب وما يجيه شي، ويظن
الشغل ضايع — وهذا صار فعلاً.

⚠️ **بس بشرط:** ما ينداز شي على `prod` إلا بعد ما ينجح
`npx tsc --noEmit -p tsconfig.app.json` و`npx eslint src` و
`npm run build` — وينفحص بمتصفح حقيقي على سيرفر شغّال، مو بالبناء
لحاله. هذا سيرفر إنتاج، والي ينكسر بيه يوقف موظفين عن شغلهم.

⚠️ **`prod` لازم يبقى fast-forward** من فرع التطوير. لو تقدّم
لحاله (صاحب النظام يشتغل عليه من واجهة GitHub أحياناً — مثل شغل
شاشة الدخول)، **ادمجه بفرع التطوير أول** قبل أي دفع، وإلا شغله
ينمسح.

### التذكير للموظف بعد الدفع

أي تعديل بالواجهة ما ينشاف بلا `npm run build` على السيرفر —
اذكرها بالجواب.

## ═══ التشغيل المحلي للفحص ═══

```
dockerd &                                    # لو مو شغّال
docker start pg                              # المستخدم staffmange / localpass
cd backend-go && DATABASE_URL='postgres://staffmange:localpass@localhost:5432/staffmange?sslmode=disable' \
  JWT_SECRET='local-dev-secret-key-32chars-min!!' PORT=4000 CORS_ORIGIN='http://localhost:5199' \
  go run ./cmd/api
cd frontend && npx vite --port 5199
```

⚠️ `CORS_ORIGIN` لازم يطابق منفذ Vite، وإلا الدخول يفشل بـ
«Failed to fetch» والفحص يطلع إيجابية كاذبة.
⚠️ `JWT_SECRET` إجباري (١٦ محرفاً فأكثر) — بدونه السيرفر يطلع.
⚠️ متصفح Playwright: `/opt/pw-browsers/chromium` — لا تنزّل غيره.

## ═══ دروس انكوينا بيها ═══

**الوضع الليلي بـ`frontend/src/index.css`:** القلب مركزي (متغيّرات
+ أصناف) مو `dark:` بكل شاشة. ⚠️ نفس المتغيّر يخدم النص والخلفية
(`--color-red-600` تخدم `text-red-600` و`bg-red-600`) — فالقلب
بالمتغيّر يصلّح وحدة ويكسر الثانية. اقرا التعليقات هناك قبل ما
تلمسه، وقيس التباين بالمتصفح مو بالعين.

**عائلة `gray` غير `slate`** بـTailwind — واثنينهن مستعملات.
نسيان وحدة يخلّي نص شاشات ما ينقرا.

**صيغة الشفافية صنف مستقل:** `.bg-sky-50\/70` ما تنصاد بقاعدة على
`.bg-sky-50`. الي ينستعمل بشفافية بكثرة لازم ينقلب بالمتغيّر.

**المناطق الغامقة بالوضعين** (القائمة الجانبية `.app-sidebar`)
تنستثنى من قلب الرمادي — وإلا تنعمى.

**جلب البيانات:** الجلب بمكان واحد داخل `useEffect` مع `alive`
تمنع سباق الطلبات، والتحديث اليدوي يطلب برفع عدّاد
(`setReload(n => n + 1)`) مو بنداء دالة تحدّث الحالة من جسم
الـeffect. تبديل فلتر بسرعة بلا هاي الحماية يخلّي جواب قديم يطمس
الجديد — والموظف يتخذ قرار على بيانات غلط.

**دور المالك ينقلب بالجلسة:** `role: 'OWNER'` تصير `'ADMIN'`
والدور الحقيقي بـ`actualRole`. أي فحص للمالك لازم يكون
`actualRole === 'OWNER'` — `role === 'OWNER'` ما تتحقق أبداً.
